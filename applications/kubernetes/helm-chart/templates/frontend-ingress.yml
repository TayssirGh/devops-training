apiVersion: {{.Values.ingress.frontend.apiVersion}}
kind: {{.Values.ingress.frontend.kind}}
metadata:
  name: {{.Values.ingress.frontend.metadata.name}}
  namespace: {{.Values.ingress.frontend.metadata.namespace}}
  
spec:
  rules:
  - host: {{.Values.ingress.frontend.spec.rules.host}}
    http:
      paths:
      - pathType: {{.Values.ingress.frontend.spec.rules.host.http.paths.pathType}}
        path: {{.Values.ingress.frontend.spec.rules.host.http.paths.pathType.path}}
        backend:
          service:
            name: {{.Values.ingress.frontend.spec.rules.host.http.paths.pathType.backend.service.name}}
            port:
              number: {{.Values.ingress.frontend.spec.rules.host.http.paths.pathType.backend.service.port.number}}
  ingressClassName: {{.Values.ingress.frontend.spec.rules.host.ingressClassName}}