apiVersion: v1
kind: ConfigMap
metadata:
  name: website-configmap
  namespace: training-app-frontend-prod
data:
  app-config.json: |-
    {
      "backendUrl": "https://app.backend.local/access"
    }
