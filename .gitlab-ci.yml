stages:
  - build

variables:
  WORKING_BRANCH: t-010-dev

build:
  stage: build
  script:
    - cd applications/qr && docker-compose up --force-recreate   
  only:
    variables:
      - $WORKING_BRANCH == "t-010-dev"
  tags:
    - t010-build-protected


